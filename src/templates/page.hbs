{{!--{{> header}} {{> navigation}}

<div class="container">

    {{#post}}
    <div class="row">
        <article>
            <h1>{{title}}</h1>
            {{{body}}}
        </article>
        <aside>
            {{> sidebar}}
        </aside>
    </div>
    {{/post}}

</div>

{{> footer}}--}} {{> header}}

<div class="wrapper">
    {{> sidebar}}
    <div class='content'>
        <div class='main'>
            <div class='main-inner'>
                <div class='blog-page'>
                    {{#post}}
                    <div class='text'>
                        <h1>{{title}}</h1>
                        {{{body}}}
                    </div>
                    {{/post}}
                </div>
            </div>
        </div>
    </div>

</div>

{{> footer}}